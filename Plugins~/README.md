# Plugins~

Runtime resources hidden from Unity import (tilde suffix).

Use for:
- Skill definition files
- Runtime configuration JSON
- Pre-built native libraries that get loaded at runtime

Unity ignores `Plugins~/` — files here are not imported into the AssetDatabase.
