// This file exists so `dotnet test` works out of the box.
// Replace it with your own tests.
using NUnit.Framework;

namespace __NAMESPACE__.Tests
{
    public sealed class PlaceholderTests
    {
        [Test]
        public void Template_works()
        {
            Assert.Pass("Template is alive. Replace this test.");
        }
    }
}
